# Hecos MCP Bridge

Bridge for Model Context Protocol (MCP) servers. Allows Hecos to use external tools from the MCP ecosystem. Supports both local stdio and remote HTTP servers via mcp-remote.

## Features
- Discover and install MCP tools.
- Manage tool configuration and secrets.
- Run local stdio subprocess servers.
- Connect to remote HTTP MCP servers seamlessly.
